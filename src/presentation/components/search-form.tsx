"use client";
import { useRouter } from "next/navigation"; import { useState } from "react";
export function SearchForm({initial="", category=""}:{initial?:string;category?:string}){const router=useRouter();const[q,setQ]=useState(initial); const[c,setC]=useState(category); return <form className="search-form" onSubmit={e=>{e.preventDefault();router.push(`/search?q=${encodeURIComponent(q)}&category=${encodeURIComponent(c)}`)}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="What are you looking for?"/><select value={c} onChange={e=>setC(e.target.value)}><option value="">All categories</option><option value="vehicles">Vehicles</option><option value="realestate">Real Estate</option><option value="electronics">Electronics</option><option value="construction">Construction</option></select><button>Search</button></form>}
