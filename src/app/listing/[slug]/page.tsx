import { notFound } from "next/navigation"; import { marketplace } from "@/infrastructure/container"; import { money } from "@/presentation/components/listing-card";
export default async function ListingPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const listing=await marketplace.getListing.execute(slug);if(!listing)notFound();return <main className="shell page detail"><div className="gallery"><img src={listing.images[0]?.url} alt={listing.images[0]?.alt}/></div><div className="detail-copy"><span className="muted">{listing.categoryName} · {listing.city}</span><h1>{listing.title}</h1><h2>{money(listing.price,listing.currency)}</h2><p>{listing.description}</p><dl><div><dt>Condition</dt><dd>{listing.conditionLabel}</dd></div><div><dt>Location</dt><dd>{listing.district}, {listing.city}</dd></div><div><dt>Views</dt><dd>{listing.viewCount}</dd></div></dl><aside><b>Listed by {listing.sellerName}</b><p>{listing.sellerPhone ?? "Contact seller through Malik"}</p><button>Show phone number</button></aside></div></main>}
