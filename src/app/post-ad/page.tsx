"use client";
import { useState } from "react";
export default function PostAd(){const[message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const response=await fetch("/api/listings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});setMessage(response.ok?"Your ad has been submitted for review.":"Please complete all required fields correctly.")}return <main className="shell page narrow"><h1>Post an Ad</h1><p className="muted">Create a clear listing to reach the right buyers.</p><form className="ad-form" onSubmit={submit}><label>Title<input name="title" required minLength={8} maxLength={180}/></label><label>Category<select name="categorySlug" defaultValue=""><option value="" disabled>Select a category</option><option value="cars">Cars</option><option value="apartments">Apartments</option><option value="mobile-phones">Mobile Phones</option></select></label><label>Price (AFN)<input name="price" type="number" required min="0"/></label><label>City<input name="city" required defaultValue="Kabul"/></label><label>District<input name="district"/></label><label>Description<textarea name="description" required minLength={20}/></label><label>Image URL<input name="imageUrl" type="url" placeholder="https://..." required/></label><button className="post">Publish ad</button>{message&&<p role="status">{message}</p>}</form></main>}
